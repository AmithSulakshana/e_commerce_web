import React from 'react'
import { useSelector } from 'react-redux'

const OnSale = () => {
  // const number = useSelector(store=>store.userSlice)
  return (
    <div>
    
    </div>
  )
}

export default OnSale
